# Fixado (não >=): o frontend do Streamlit é um build estático empacotado no
# wheel; queremos garantir a mesma versão testada localmente no deploy.
streamlit==1.59.2
google-genai>=2.12
supabase>=2.31
python-dotenv>=1.2
pydantic>=2.13
Pillow>=12.3
pytest>=9.1
